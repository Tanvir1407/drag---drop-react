export default function Title() {
  return (
    <div>
      <h1 className="text-white text-3xl">Creating basic Drag & Drop 👆 </h1>
      <span>( without external libraries )</span>
    </div>
  );
}
